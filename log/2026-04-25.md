# Log for 2026-04-25

## Summary

Expanded the Dafny `mini_pilot` from a tiny 3-seed check into a broader B1/B2-focused pilot that is large enough to stress-test the paper's metric definitions, failure-label taxonomy, and Section 5 table shapes.

## Work Completed

- Broadened the pilot seed set using official `dafny-lang/dafny` examples that verify on local Dafny `4.11.0`
  - total seeds now: `8`
  - strata now represented:
    - `benchmark_example_suite`
    - `proof_engineering_case`
- Added five new official seeds:
  - `queens`
  - `deque`
  - `twowaysort`
  - `ringbuffer`
  - `bfs`
- Updated `mini_pilot/seed_metadata.csv`
  - recorded stratum and proof-profile metadata for all 8 seeds
- Expanded the manual transformation matrix
  - total transformed items now: `24`
  - refactoring classes represented:
    - `rename`
    - `helper_extraction`
    - `statement_reordering`
    - `ghost_code_movement`
- Added new transformed and B2-follow-up Dafny files under:
  - `mini_pilot/transformed/`
  - `mini_pilot/b2/`
- Updated `mini_pilot/manifest.csv`
  - kept it as the single source of truth for transformed items
  - documented refactoring rationale and expected proof impact per item
- Reworked `mini_pilot/run_pilot.py`
  - kept manifest-driven execution
  - preserved explicit outcome vocabulary:
    - `pass`
    - `fail`
    - `timeout`
    - `excluded`
    - `not_run`
  - added derived outputs:
    - `mini_pilot/summary_by_class.csv`
    - `mini_pilot/summary_by_stratum.csv`
  - strengthened memo generation with:
    - class coverage
    - stratum coverage
    - label coverage
    - sparse-cell warnings
    - ready / not-ready judgments
- Updated `mini_pilot/README.md` to match the broader pilot

## Larger Pilot Outcomes

- Seeds verified locally: `8/8`
- Transformed items attempted: `24`
- Eval-ready items: `24`
- Broken under B1: `9`
- Timed out under B1: `0`
- Excluded before evaluation: `0`
- Recovered by B2: `4`
- Residual unresolved after available baselines: `5`
- B3 status: deferred, recorded as `not_run`

## Coverage Notes

- Strata represented:
  - `benchmark_example_suite`: `5` seeds, `15` items
  - `proof_engineering_case`: `3` seeds, `9` items
- Class-level summary:
  - `ghost_code_movement`: `3` attempted, `2` broken, `0` recovered by B2
  - `helper_extraction`: `8` attempted, `7` broken, `4` recovered by B2
  - `rename`: `8` attempted, `0` broken
  - `statement_reordering`: `5` attempted, `0` broken
- Failure labels observed on broken items:
  - `brittle_lemma_or_helper_application_structure`
  - `broken_assertion_flow`
  - `ghost_code_or_proof_hint_displacement`
  - `weakened_loop_invariants`

## Metric Validation Notes

- Metrics that now look workable at broader pilot scale:
  - `SR`
  - `BR(class_i)`
  - `RR(B2 | broken)`
  - class-level and stratum-level verifier cost summaries
- Metrics / conventions that still need paper-side refinement:
  - `RR(B1)` is still structurally weak because B1 defines the broken subset
  - `Gap_residual` still needs an explicit “available baselines” convention while B3 is deferred
  - `Cost_tok(B3)` remains unvalidated without a real B3 pass

## Table Validation Notes

- Benchmark composition table now works meaningfully because the pilot has:
  - multiple strata
  - enough items to avoid single-row degeneracy
- Breakage / recovery table now gives useful class-level signal
  - especially around the asymmetry between `helper_extraction` and the more robust classes
- Failure-mode table is now informative with four active labels
  - but two labels are still sparse:
    - `broken_assertion_flow`
    - `weakened_loop_invariants`
- Main table-design conclusion:
  - current Section 5 table shapes are workable
  - but low-frequency classes or labels may still need merging or demotion before final paper tables are frozen

## Decision Output

- Are the current metrics workable?
  - Yes, with caveats around `RR(B1)` and `Gap_residual`
- Are the current labels workable?
  - Yes for pilot-scale characterization
- Are the current table shapes workable?
  - Yes, much more clearly than in the 9-item pilot
- What must change before adding B3 or scaling further?
  - add an explicit available-baselines interpretation rule
  - keep `not_run` first-class in paper tables
  - preserve `excluded` separately from genuine verification failures

## Files Added or Updated

- `mini_pilot/README.md`
- `mini_pilot/seed_metadata.csv`
- `mini_pilot/manifest.csv`
- `mini_pilot/run_pilot.py`
- `mini_pilot/seed_results.csv`
- `mini_pilot/results.csv`
- `mini_pilot/summary_by_class.csv`
- `mini_pilot/summary_by_stratum.csv`
- `mini_pilot/pilot_memo.md`
- `mini_pilot/seeds/*.dfy`
- `mini_pilot/transformed/*.dfy`
- `mini_pilot/b2/*.dfy`

## Next Useful Steps

- Update the paper's Section 4/5 metric wording to reflect:
  - `RR(B2 | broken)` as the informative recovery quantity in early experiments
  - `Gap_residual` parameterized by enabled baselines
  - `not_run` as an explicit table value
- Decide whether to keep all four refactoring classes separate in paper tables or merge sparse rows later
- Run a targeted B3 pass on the broken subset to validate:
  - retry-count handling
  - token-cost logging
  - the full residual-gap formula used in the paper
